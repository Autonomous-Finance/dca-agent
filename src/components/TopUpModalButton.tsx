import React from 'react'

function TopUpModalButton() {
  return (
     {/* <TextField
      sx={{ flexShrink: 1 }}
      disabled={loading}
      size="small"
      value={amount}
      onChange={(e) => setAmount(e.target.value)}
      type="number"
      label="Desposit"
      InputProps={{
        endAdornment: (
          <InputAdornment position="end">{credSymbol}</InputAdornment>
        ),
      }}
      error={error !== ""}
      helperText={error}
    /> */}
  )
}

export default TopUpModalButton